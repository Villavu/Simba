program WriteLn_ExampleThree;

var
  I: Integer;

begin
  I := 100;

  Writeln(IntToStr(I));
  // OR..
  Writeln(I);
  // The above will only work if you only want to print one thing.
    //  (see WriteLn example #4 on how to overcome this)
end.
