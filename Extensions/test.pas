program new;

{ Creating TForm crashed for me, but probably because I'm on Linux x86_64 }
procedure init;

//var
//    a: TForm;
begin
    createf;
//  a := TForm.Create(nil);
//  a.ShowModal;
//  a.Free;
end;

begin
end.
